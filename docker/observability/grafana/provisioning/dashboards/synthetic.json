{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    {
      "title": "Probe Success State",
      "type": "stat",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [
        {
          "expr": "probe_success",
          "legendFormat": "{{target}}"
        }
      ]
    },
    {
      "title": "Probe Latency ms",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "targets": [
        {
          "expr": "probe_latency_ms",
          "legendFormat": "{{target}}"
        }
      ]
    }
  ],
  "refresh": "5s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["synthetic", "e17"],
  "time": { "from": "now-1h", "to": "now" },
  "title": "Synthetic"
}
